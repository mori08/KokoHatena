[[Record]] # 年（セーブデータ用）
name    = "RecordYear"
digit   = 7
default = 0
[[Record]] # 月（セーブデータ用）
name    = "RecordMonth"
digit   = 4
default = 1
[[Record]] # 日（セーブデータ用）
name    = "RecordDate"
digit   = 5
default = 1
[[Record]] # 時（セーブデータ用）
name    = "RecordHour"
digit   = 5
default = 0
[[Record]] # 分（セーブデータ用）
name    = "RecordMinute"
digit   = 6
default = 0

[[Record]] # ゲーム内の経過日数
name    = "Date"
digit   = 5
default = 0